bins:
  poly_id: group_by
  release_time: 86400

# This line only counts the first occurrence of each particle
filter_particle: active > 0

infile: {ladim_output_file}
outfile: {crecon_output_file}

output_varname: pcount
